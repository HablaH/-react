import React, { Component } from 'react';
class ScoreButton extends Component {
    render() { 
        const upDown = {if(this.props.)}
        return (
            <button>
                +
            </button>
        );
    }
}
 
export default ScoreButton;